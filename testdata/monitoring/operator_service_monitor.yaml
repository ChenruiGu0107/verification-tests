apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: example
  labels:
    k8s-app: prometheus
spec:
  endpoints:
    - port: web
      interval: 30s
  selector:
    matchLabels:
      k8s-app: prometheus