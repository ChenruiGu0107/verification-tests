apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: drill.rule
spec:
  groups:
  - name: Test rules
    rules:
    - alert: DrillAlert
      expr: vector(1)
      labels:
        severity: warning