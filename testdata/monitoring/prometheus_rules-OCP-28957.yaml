apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: ocp-28957.rules
spec:
  groups:
  - name: Test rules
    rules:
    - alert: Watchdog
      expr: vector(1)
      labels:
        severity: none