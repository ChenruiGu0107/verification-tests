apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: descheduler-cronjob-ocp29821
  namespace: kube-system
spec:
  schedule: "*/2 * * * *"
  concurrencyPolicy: "Forbid"
  jobTemplate:
    spec:
      template:
        metadata:
          name: descheduler-pod
        spec:
          priorityClassName: system-cluster-critical
          containers:
          - name: descheduler
            image: registry.stage.redhat.io/openshift4/ose-descheduler:4.3
            volumeMounts:
            - mountPath: /policy-dir
              name: policy-volume
            command:
              - "/bin/descheduler"
            args:
              - "--policy-config-file"
              - "/policy-dir/policy.yaml"
              - "--v"
              - "3"
          restartPolicy: "Never"
          serviceAccountName: descheduler-sa-ocp29821
          volumes:
          - name: policy-volume
            configMap:
              name: descheduler-policy-configmap-ocp29821

