kind: DaemonSet
apiVersion: apps/v1
metadata:
  name: flex-deploy
spec:
  selector:
    matchLabels:
      app: flex-deploy
  template:
    metadata:
      labels:
        app: flex-deploy
    spec:
      serviceAccount: flex-deployer
      containers:
        - name: flex-deploy
          image: quay.io/openshifttest/flexvolume@sha256:5abab5e2f3cb8879e8cce80b0746d55ab28f8f07cb0612da8da8cf0ec4e6b046
          securityContext:
            privileged: true
            capabilities:
              add: ["SYS_ADMIN"]
            allowPrivilegeEscalation: true
          volumeMounts:
            - name: mountpoint-dir
              mountPath: /flexmnt
      volumes:
        - name: mountpoint-dir
          hostPath:
            path: /etc/kubernetes/kubelet-plugins/volume/exec/
