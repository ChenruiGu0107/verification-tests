apiVersion: v1
kind: Secret
metadata:
  name: "my-aws-secret"
data:
  aws-access-key-id: <%= cb.cloud_creds["aws_access_key_id"] %>
  aws-secret-access-key: <%= cb.cloud_creds["aws_secret_access_key"] %>
