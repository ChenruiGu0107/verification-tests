apiVersion: v1
kind: Pod
metadata:
  name: cli
  labels:
    name: cli
spec:
  restartPolicy: OnFailure
  containers:
  - name: cli
    image: quay.io/openshifttest/oc-cli:tagname
    command: [ "oc" ]
    args: 
      - "adm"
      - "prune"
      - "images"
      - "--force-insecure=true"
