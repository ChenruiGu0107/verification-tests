module CucuShift
  class CliExecutor

    # @param [CucuShift::User] user user to execute command with
    def exec(user, key, opts={})
      raise
    end
  end

  class MasterOsCliExecutor
  end
end
